# 1С WebCamera Capture DLL

C# DLL для захвата видео с веб-камеры для интеграции с 1С:Предприятие.

## 🎯 Возможности

- ✅ Захват кадров с веб-камеры
- ✅ Список доступных устройств захвата видео
- ✅ Выбор камеры для использования
- ✅ Конвертация кадров в JPEG
- ✅ COM интеграция с 1С
- ✅ Обработка ошибок

## 📋 Требования

- Windows 7 или выше
- . NET Framework 4.7.2+
- Веб-камера, подключённая к системе
- 1С:Предприятие 8.2 или выше

## 🚀 Установка

### 1. Сборка проекта

```bash
git clone https://github.com/ipanshar/1c-webcam-capture. git
cd 1c-webcam-capture
dotnet build -c Release
```

### 2.  Регистрация COM (от администратора)

```bash
cd bin\Release\net472
regasm WebCameraCapture.dll /codebase
regasm WebCameraCapture.dll /tlb:WebCameraCapture.tlb
```

### 3.  Проверка регистрации

```bash
# Выведет информацию о зарегистрированном COM объекте
reg query HKEY_CLASSES_ROOT\WebCameraCapture.WebCameraCapture
```

## 💻 Использование в 1С

```1c
// Создание объекта
КамераOLE = Новый COMОбъект("WebCameraCapture.WebCameraCapture");

// Инициализация
КамераOLE.Initialize();

// Получение списка камер
СписокКамер = КамераOLE.GetDeviceList();

// Выбор камеры (индекс 0)
КамераOLE.SetCamera(0);

// Захват кадра
ДвоичныеДанные = КамераOLE.CaptureFrame();

// Сохранение кадра
ФайлВывода = Новый ЗаписьДвоичныхДанных("C:\photo.jpg");
ФайлВывода.Записать(ДвоичныеДанные);
ФайлВывода.Закрыть();

// Очистка ресурсов
КамераOLE.Cleanup();
```

## 📚 API

### IWebCameraCapture

#### Initialize() → bool
Инициализирует захват видео.  Возвращает `true` при успехе. 

#### CaptureFrame() → byte[]
Захватывает текущий кадр с камеры в формате JPEG. Возвращает массив байтов.

#### GetDeviceList() → string[]
Возвращает список всех доступных видеоустройств. 

#### SetCamera(int index) → bool
Выбирает камеру по индексу.  Возвращает `true` при успехе.

#### GetCameraCount() → int
Возвращает количество доступных камер.

#### GetErrorMessage() → string
Возвращает текст последней ошибки.

#### Cleanup() → void
Освобождает все ресурсы и закрывает соединение с камерой.

## 🔧 Отладка

### Проверка работы

```1c
КамераOLE = Новый COMОбъект("WebCameraCapture.WebCameraCapture");

Если КамераOLE.Initialize() Тогда
    Сообщение("Инициализация успешна");
    Сообщение("Камер найдено: " + КамераOLE.GetCameraCount());
Иначе
    Сообщение("Ошибка: " + КамераOLE.GetErrorMessage());
КонецЕсли;

КамераOLE. Cleanup();
```

### Распространённые проблемы

**Проблема:** "Class not registered"
- **Решение:** Пересоберите DLL и перезарегистрируйте COM

**Проблема:** "No video capture devices found"
- **Решение:** Убедитесь, что камера подключена и работает

**Проблема:** "Access Denied" при регистрации
- **Решение:** Запустите Command Prompt от администратора

## 📦 Структура проекта

```
1c-webcam-capture/
├── src/
│   └── WebCameraCapture.cs
├── examples/
│   └── example_1c.bsl
├── WebCameraCSharp.csproj
├── README.md
└── LICENSE
```

## 📄 Лицензия

MIT License - см. LICENSE файл

## 👨‍💻 Автор

ipanshar

## 🤝 Вклад

Приветствуются pull requests и issues! 